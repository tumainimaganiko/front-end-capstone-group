import React from 'react';
import Slider from '../components/Slider';

const Cars = () => (
  <section className="flex justify-center items-center h-screen z-10">
    <Slider />
  </section>
);

export default Cars;
